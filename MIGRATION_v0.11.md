# v0.11 데이터 이전 안내

1. Node.js 22로 변경합니다.
2. `.env.example`의 v0.11 환경변수를 `.env`와 Render에 추가합니다.
3. `firestore.rules.example`을 참고해 사용자 캐시, 피드백, 관리자 검토함 규칙을 배포합니다.
4. 기존 학습 통계는 유지됩니다.
5. v0.11 이후 문제풀이부터 `attemptEvents`와 `users/{uid}/cbtAttempts`에 원본 이벤트가 생성됩니다.
6. 학습 기록 → 학습 데이터 관리에서 `통계 다시 계산`을 실행하면 원본 이벤트가 존재하는 범위만 정확히 재생성됩니다.
7. v0.11 이전 누적 기록은 원본 선택 답안 이벤트가 없으므로 완전한 역산이 불가능합니다. 기존 기록을 보존하거나 필요한 자격증만 초기화한 뒤 다시 학습하세요.

## AI 해설 캐시

v0.10 브라우저 캐시는 서버 서명이 없으므로 v0.11에서 자동으로 신뢰하지 않습니다. 처음 한 번 다시 생성한 뒤 서명된 캐시가 Firestore와 브라우저에 저장됩니다.
