# MakerOS Product Alpha v0.10 이전 안내

## 데이터베이스 이전

필수 Firestore 구조 변경은 없습니다. 기존 CBT 문제와 학습 기록을 그대로 사용할 수 있습니다.

## 주제별 출제 빈도

기존 `3회 이상`, `5회 이상`, `10회 이상` 필터는 문제 개수가 아니라 선택 연도 범위에서 해당 주제가 나온 **서로 다른 시험 문서 수**를 기준으로 계산합니다.

- 전체: 출제 시험 수와 관계없이 표시
- 3개 시험 이상: 서로 다른 시험 3개 이상에서 등장
- 5개 시험 이상: 서로 다른 시험 5개 이상에서 등장
- 10개 시험 이상: 서로 다른 시험 10개 이상에서 등장

선택 범위에 등록된 시험 수보다 큰 조건은 비활성화됩니다.

## AI 해설

등록 해설이 없는 텍스트형 문제는 답안 공개 후 `/api/cbt/generate-explanation`을 자동 호출합니다.

1. 1차 AI가 공식 정답을 변경하지 않고 해설 초안을 생성합니다.
2. 2차 AI가 문제·선택지·공식 정답·초안을 독립적으로 대조합니다.
3. 서버 코드가 두 응답의 정답 번호와 명시적인 정답 표현을 마지막으로 검사합니다.
4. 하나라도 충돌하면 공식 정답만 유지하고 AI 해설은 표시하지 않습니다.

검증 완료 해설은 브라우저 LocalStorage에 문제·선택지·정답 해시 기준으로 저장됩니다. 문제나 정답이 바뀌면 기존 캐시를 사용하지 않고 다시 생성합니다.

이미지 문제는 현재 이미지 내용까지 검증하지 않으므로 자동 해설을 차단합니다.

## 환경변수

기존과 동일하게 서버의 `.env`에 아래 값을 설정해야 합니다.

```env
GEMINI_API_KEY=...
GEMINI_MODEL=gemini-3.5-flash-lite
```
