# MakerOS Alpha v0.8 마이그레이션

## 1. 주제 자동 분류

기존 문제에 `topic`, `chapter`, `unit`, `category`, `subTopic`, `keyword`, `tags`가 없어도 앱이 문제 문장·보기·해설의 핵심어를 분석해 주제를 생성합니다.

우선순위:

1. 관리자가 입력한 주제 메타데이터
2. 등록된 태그
3. 과목별 핵심어 규칙
4. 문제 문장에서 추출한 대표 핵심어
5. 과목별 기타 핵심개념

따라서 기존 Firestore 문제를 다시 업로드하지 않아도 조회 시 자동 분류됩니다. 새로 업로드하는 문제는 분류 결과가 `topic`, `topicSource`, `topicConfidence`, `tags`에 함께 저장됩니다.

## 2. 기존 `미분류 주제` 기록

앱 시작 시 `learningProgress`의 기존 `미분류 주제` 기록을 문제 문장과 보기를 기준으로 다시 분류합니다. 과거 세션 단위 기록 중 문제 본문이 없어 재분류할 수 없는 `미분류 주제` 기록은 새 주제 진도 계산에서 제외됩니다.

## 3. 시험 준비도

시험 준비도는 단순 점수 평균이 아니라 다음 요소를 사용합니다.

- 기출 범위 학습률: 35%
- 기출 회차 범위: 25%
- 반복 복습을 반영한 문제 숙련도: 25%
- 실전 시험형 성적 검증: 15%

기본 목표는 최근 3~4개년도에 해당하는 약 12회차입니다. 등록 기출이 12회보다 적으면 현재 등록된 회차를 앱 내부 범위 목표로 사용하면서, 화면에 등록 회차 부족을 안내합니다.

## 4. 권장 일일 문제

권장 일일 문제 수는 다음을 합산해 남은 학습일로 나눕니다.

- 아직 풀지 않은 목표 기출 문제
- 풀었지만 마스터하지 못한 문제
- SRS 복습 예정 문제
- 반복 오답 문제

시험일을 설정하지 않은 경우에는 90일 완성 계획을 기준으로 계산합니다. 시험 직전 7일 이내이고 준비도가 낮으면 한 회차 수준의 실전 학습량을 우선 권장합니다.
