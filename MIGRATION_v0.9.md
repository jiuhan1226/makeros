# v0.9 적용 안내

## Firestore 재업로드 여부
필요하지 않습니다. 세부 주제 통합은 화면과 학습 분석 단계에서 현재 문제 목록을 기준으로 계산합니다.

## 기존 학습 기록
기존 기록의 `questionId`를 현재 문제 목록과 연결하여 통합 주제명으로 표시합니다. 원본 `topic` 필드는 삭제하지 않습니다.

## 통합 기준
1. 같은 과목에서 기존 주제에 2문제 이상 있으면 유지
2. 2문제 미만이면 문제 내용 기반 규칙 주제로 재분류
3. 같은 규칙 주제에 2문제 이상 모이면 통합
4. 다빈도 주제와 텍스트 유사도가 충분하면 해당 주제로 통합
5. 나머지는 과목별 기타 핵심개념으로 통합

## 준비도 원형 바
`LearningCenterPage.jsx`의 SVG 원형 진행률과 `styles.css`의 `.pass-ring-*` 스타일을 함께 사용합니다.
