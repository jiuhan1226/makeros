# MakerOS v3.1 변경 사항

## v3.1.12 전국 학교 시간표·급식·PDF별 개념카드

- 나이스 교육정보 개방 API를 연결해 학교명으로 전국 학교를 검색하고 학교를 전환하도록 개선
- 선택한 학교·주간·학년·반 기준으로 실제 시간표를 조회하며 학교 유형에 따라 학년 선택 범위를 자동 조정
- 학교마다 다른 반 수와 수업 편성을 고려해 최대 20개 반과 조회 결과의 추가 교시를 표시
- 선택한 학교의 조식·중식·석식, 열량, 알레르기 번호, 영양·원산지 정보를 확인하는 급식 탭 추가
- 학급 시간표에 교사명을 한 번 등록하면 저장된 학급 수업을 교사별 시간표로 묶어 보는 기능 추가
- 공식 나이스 시간표 API에 교사명이 포함되지 않는 제한을 화면에 명시
- 개념카드를 PDF 고유 ID별 독립 카드 묶음으로 분리해 같은 파일명의 다른 자료도 섞이지 않도록 개선
- 학교 API 응답 변환과 PDF 카드 묶음 분리를 검증하는 자동 테스트 추가

## v3.1.11 일정·시간표·개념카드 학습

- 캘린더에서 직접 추가한 일정과 목표·자격증 일정을 눌러 개별 색상을 지정하고 저장
- 학년·반·교시별 주간 시간표 화면 추가
- 교시별 과목·선생님 편집 지원
- 캘린더와 시간표, 시간표와 오늘 공부 화면을 서로 바로 이동하도록 연결
- 개념카드에 `새 카드 / 학습 중 / 암기 완료` 상태와 학습·정답 횟수 저장
- 폴더별 카드 학습 세션, 다시 보기·알고 있어요 분류, 선택형 퀴즈, 완료 결과 화면 추가
- 카드 학습 로직과 일정 색상·시간표 상태 보존 자동 테스트 추가

## 제품 중심 변경

- 기본 시작 화면을 기존 모듈 홈에서 **AI 파트너 오늘 화면**으로 변경
- 상단 핵심 정보구조를 `오늘 / 계획 / 캘린더 / 학습 / 목표`로 재정의
- 기존 MakerOS 기능은 모바일 전체 메뉴의 `기존 MakerOS 기능`에서 계속 접근 가능

## 신규 구현

- 학생 프로필: 학년, 전공, 주간/요일별 가능 시간, 고정 일정
- 내신 목표: 현재점수, 목표점수, 시험일, 취약 단원
- 자격 목표: 종목, 상태, 시험일, 최근 CBT 정답률
- 취업 목표: 산업, 회사, 직무, 지원 시기, 필요 역량
- 활동 목표: 최대 2개 대회/프로젝트와 마감
- 사용자가 입력한 목표 기간 기반 로드맵 + 주간 계획 + 오늘 계획
- 계획 `draft / active / superseded / discarded` 버전 관리
- 변경안 diff, 학생 확정, 보류, 롤백
- CBT 점수 저장 후 자격증 계획 자동 재계산 초안 생성
- 오늘 행동 완료 후 재계획 초안 생성
- 통합 마감 캘린더
- 규칙 기반 fallback + Gemini 보조 계획 API
- 파트너 계획 스모크 테스트

## 안전 원칙

- AI 계획이 기존 계획을 자동 덮어쓰지 않음
- 학생 확정 전에는 항상 draft
- 일/주 가능 시간 초과 시 클라이언트 검증에서 축소
- AI 호출 실패 시 규칙 기반 계획 사용
- 공식 CBT 점수는 기존 공식 정답 채점 로직을 그대로 사용
